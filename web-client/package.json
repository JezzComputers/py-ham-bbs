{
  "name": "test_site",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "build": "tsup src --format esm --minify --out-dir dist",
    "serve": "caddy run --config Caddyfile --adapter caddyfile"
  },
  "keywords": [],
  "author": "Jeremy & Luke",
  "license": "BSD-3-Clause",
  "type": "module",
  "dependencies": {
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  }
}
